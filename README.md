# solo
#include <stdio.h>
#include<math.h>

int main() {
   int a=5   ;
   int b=2;
   int c=27;
   int d=25;
   int powr= pow(a,b);
   int rac= sqrt(d);
   int cub=cbrt(c);
   printf("power=%d\n",powr);
   printf("racin=%d\n",rac);
   printf("cubique=%.2lf\n",cbrt(c));
   printf("%d ",c);
   printf(" %d",cub);
   
    return 0;
}
